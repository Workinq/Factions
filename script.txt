classpath   "%classpathLibs%"
            "%compileArtifactPath%"
            "%pluginArtifactPath%"
            ;

open        "%project.build.directory%%file.separator%%project.build.finalName%.%project.packaging%";

trimExclude none;

exclude     *. and          // exclude all packages
            *.* and         // exclude all classes
            *.* * and       // exclude all fields
            *.* *(*)        // exclude all methods
            ;

// Faction Missions
unexclude   com.massivecraft.factions.mission. and
            com.massivecraft.factions.mission.missions. and
            com.massivecraft.factions.mission.* and
            com.massivecraft.factions.mission.missions.* and
            com.massivecraft.factions.mission.* * and
            com.massivecraft.factions.mission.missions.* * and
            com.massivecraft.factions.mission.* *(*) and
            com.massivecraft.factions.mission.missions.* *(*)
            ;

// Faction Upgrades
unexclude   com.massivecraft.factions.upgrade. and
            com.massivecraft.factions.upgrade.upgrades. and
            com.massivecraft.factions.upgrade.* and
            com.massivecraft.factions.upgrade.upgrades.* and
            com.massivecraft.factions.upgrade.* * and
            com.massivecraft.factions.upgrade.upgrades.* * and
            com.massivecraft.factions.upgrade.* *(*) and
            com.massivecraft.factions.upgrade.upgrades.* *(*)
            ;

// Inventory Actions
unexclude   com.massivecraft.factions.action. and
            com.massivecraft.factions.action.* and
            com.massivecraft.factions.action.* * and
            com.massivecraft.factions.action.* *(*)
            ;

// Utility Classes
unexclude   com.massivecraft.factions.util. and
            com.massivecraft.factions.util.* and
            com.massivecraft.factions.util.* * and
            com.massivecraft.factions.util.* *(*)
            ;

obfuscateFlowExclude none;

stringEncryptionExclude none;

obfuscate   changeLogFileOut="%project.build.finalName%-Changelog.txt"
            aggressiveMethodRenaming=true
            keepInnerClassInfo=false
            keepGenericsInfo=true
            obfuscateFlow=aggressive
            encryptStringLiterals=none
            exceptionObfuscation=heavy
            autoReflectionHandling=normal
            obfuscateReferences=normal
            obfuscateReferenceStructures=inSpecialClass
            obfuscateReferencesPackage="factions"
            lineNumbers=scramble
            localVariables=delete
            methodParameters=obfuscate
            newNamesPrefix="KR"
            mixedCaseClassNames=ifInArchive
            randomize=true
            allClassesOpened=true
            deriveGroupingsFromInputChangeLog=false
            keepBalancedLocks=true
            preverify=true
            assumeRuntimeVersion="8"
            legalIdentifiers=true
            hideFieldNames=true
            hideStaticMethodNames=true
            ;

saveAll     "%project.build.directory%%file.separator%%project.build.finalName%.%project.packaging%";
